# requirements.txt (final)
flask==2.3.3
werkzeug==2.3.7
gunicorn==21.2.0

# OpenAI client + HTTP stack
openai==1.51.2
httpx==0.27.2
python-dotenv==1.0.1

# Markdown stack (compatible with Python 3.13)
markdown==3.4.4

# Other libs in use
pdfkit==1.0.0
# secure-smtplib is not needed for Python 3's smtplib
